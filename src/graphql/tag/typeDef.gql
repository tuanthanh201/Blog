type Tag {
  id: ID!
  content: String!
  createdAt: String!
}
extend type Query {
  findTagById(tagId: ID!): Tag
  findTagsByContent(content: String!): [Tag]
  findTagByContentExact(content: String!): [Tag]
  findAllTags: [Tag]
}
